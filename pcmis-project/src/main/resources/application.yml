server:
  port: 9070

spring:
  application:
    name: pcmis-project
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        namespace: pcmis
        group: pcmis
      
feign:
  sentinel:
    enabled: true

