server:
  port: 9030

spring:
  application:
    name: pcmis-visual
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        namespace: pcmis
        group: pcmis
      
feign:
  sentinel:
    enabled: true

