server:
  port: 9080

spring:
  application:
    name: pcmis-file
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        namespace: pcmis
        group: pcmis
      
feign:
  sentinel:
    enabled: true

