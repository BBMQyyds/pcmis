server:
  port: 9060

spring:
  application:
    name: pcmis-risk
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        namespace: pcmis
        group: pcmis
      
feign:
  sentinel:
    enabled: true

